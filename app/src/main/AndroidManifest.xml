<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.crdiggs.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".TeacherInfo" />
        <activity
            android:name=".StudentInfo"
            android:label="@string/title_activity_student_info"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Competencies" />
        <activity android:name=".Subcompetencies" />
        <activity android:name=".CompetencyList" />
        <activity android:name=".EditCompetency"></activity>
    </application>

</manifest>